import React from "react";
import * as styles from "./com.module.css";

const About: React.FC = () => {
    return (
        <section className={styles.t} id="t" data-aos="fade-up">

        </section>
    );
};

export default About;
